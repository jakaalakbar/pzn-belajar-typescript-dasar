describe("data type", () => {
  it("should must declare", () => {
    let name: string = "Jaka Al Akbar";
    let balance: number = 11000;
    let isVip: boolean = true;

    console.info(name);
    console.info(balance);
    console.info(isVip);
  });
});
